class Monedas { 
 // PROPIEDADES - variables
  // variables para posición
  // variables para la imagen

  // -----------------------------------------------------
  // CONSTRUCTOR > el "setup de la clase"
  // inicializar las propiedades/variables
  // todas por random()
  // IMAGENES
  // --carga de las imagenes con un for

  // -----------------------------------------------------
  // METODOS (funciones)
  
  // actualizarMonedas()
  // -- mueve las monedas y llama a reciclar()

  // dibujarMonedas()
  // --dibuja las monedas en la posicion x/y de la clase

  // reciclarMonedas()
  // --recicla todas las monedas (valor inicial o random)

}
