class Vidas {
  // PROPIEDADES - variables
  // variables para posición
  // variables para la imagen

  // -----------------------------------------------------
  // CONSTRUCTOR > el "setup de la clase"
  // inicializar las propiedades/variables
  // todas por random()
  // IMAGENES
  // --carga de las imagenes de las tres vidas con un for

  // -----------------------------------------------------
  // METODOS (funciones)
  
  // actualizarVidas()
  // -- llama a reciclar() a las vidas para volver a comenzar la partida??

  // dibujarVidas()
  // --dibuja las vidas en la posicion x/y de la clase

  // reciclarVidas()
  // --recicla a las vidas para volver a utilizarlas en otra partida (valor inicial o random) 
  
  
}
