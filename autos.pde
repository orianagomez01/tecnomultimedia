class Autos {
 // PROPIEDADES - variables
  // variables para posición
  // variables para la imagen
//LA CLASE AUTOS SERÍA DE LOS ENEMIGOS

  // -----------------------------------------------------
  // CONSTRUCTOR > el "setup de la clase"
  // inicializar las propiedades/variables
  // todas por random()
  // IMAGENES
  // --carga de las imagenes de los autos con un for

  // -----------------------------------------------------
  // METODOS (funciones)
  
  // actualizarAutos()
  // -- llama a reciclar() a los autos 

  // dibujarAutos()
  // --dibuja a los autos en la posicion x/y de la clase

  // reciclarAutos()
  // --recicla a los autos (valor inicial o random)
}
